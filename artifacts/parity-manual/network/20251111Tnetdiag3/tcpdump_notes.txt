[lo0 9080 capture]
- IPv4 (07:49:39 JST+?): SYN/SYN-ACK/ACK 完了後、GET 115byte を送出。サーバー(127.0.0.1.9080) は ACK のみ返し HTTP payload を一切返さず 15s 間隔で keep-alive ACK を継続。07:51:38 頃クライアント FIN→サーバー ACK、直後に RST 応答。
- IPv6 (07:51:46〜): ::1 側も SYN/SYN-ACK/ACK までは完了しリクエスト payload を送出済。ただしこちらも ACK のみでアプリ応答なし。約30s 経過後クライアント FIN、サーバー ACK。
- いずれも TCP レイヤでは握手成功・リクエスト転送済みだが、アプリ層応答が生成されずタイムアウト待ちになる状況。
[socat 19080 capture]
- IPv6 で 19080 へ接続すると socat 側が RST を即返す（::1 リッスン未設定）。
- IPv4 19080→socat→9080 経路では 19080 経路/9080 下流ともに SYN/ACK/HTTP 送信までは通るが、下流 9080 側からレスポンス payload が来ず ACK のみ。結果として socat 側も転送できず同じくタイムアウト。
