# SP4 main scenarios (Charts/Reception/Claim)
runId: 20251124T073245Z
parentRunId: 20251124T000000Z
flag: WEB_ORCA_MASTER_SOURCE=server + VITE_DISABLE_MSW=1 (Stage/Preview, VITE_DEV_PROXY_TARGET=http://100.102.17.40:8000/openDolphin/resources)
status: partial
command: RUN_ID=20251124T073245Z VITE_DEV_PROXY_TARGET=http://100.102.17.40:8000/openDolphin/resources VITE_API_BASE_URL=http://100.102.17.40:8000/openDolphin/resources VITE_DISABLE_MSW=1 PLAYWRIGHT_BASE_URL=https://localhost:4173 WEB_ORCA_MASTER_SOURCE=server npm run e2e:orca-master
notes:
executedAt: 2025-11-26T05:47:12+0900
notes:
  - 2025-11-26T05:47:12+0900 Stage/Preview Vite stage server (https://localhost:4173 via `VITE_DEV_PROXY_TARGET=http://100.102.17.40:8000/openDolphin/resources`, MSW disabled) replayed the Live profile Charts scenario once, emitted the badge/警告 warning tone for the real `dataSource=server` response, and recorded `dataSourceTransition=snapshot→server`/`cacheHit=false`/`missingMaster=false`/`fallbackUsed=false` metadata while `recordPerfLog` captured runId `20251124T073245Z` for the telemetry pipeline.
  - Reception/Claim flows continue to be gated by `profile==='msw'` plus `test.skip`/`test.fixme`, so the Stage run still exercises the MSW snapshot path that reports `missingMaster=true`/`fallbackUsed=true` and the caution-tone warning banner. They will need another re-run once the Live base URLs (`/reception/insurance`, `/claim/tensu`, etc.) are reachable and the skip/fixme guards can be removed so we can capture those server-run warning tones and `dataSourceTransition` shifts.
  - 2025-11-25T19:23:30+0900: prior Stage run (same command, PLAYWRIGHT_BASE_URL=http://localhost:4173) also produced Chart-only results; `artifacts/e2e/20251124T073245Z/sp4-main-scenarios.log` is the single source of truth for Stage log updates, so we overwrite it each time we re-run.
scenarios:
  - name: Charts master bridge
    apiRoute: /orca/master/*
    dataSource: server
    cacheHit: false
    missingMaster: false
    fallbackUsed: false
    status: pass
    note: Live scenario visited `/charts/72001`, the warning badge displayed the `warning` tone, and `recordPerfLog`/audit metadata confirmed the server payload (dataSource transition from `snapshot` to `server`).
  - name: Reception insurance/address lookup
    apiRoute: /orca/master/{hokenja,address}
    dataSource: snapshot
    cacheHit: false
    missingMaster: true
    fallbackUsed: true
    status: msw-only skip
    note: Reception scenarios remain behind MSW fixtures (`test.skip(profile !== 'msw')` / `test.fixme`), so Stage runs still fall back to snapshot data and issue the caution-tone warning banner (`missingMaster=true`, `fallbackUsed=true`).
  - name: Claim tensu/billing search
    apiRoute: /orca/tensu/ten
    dataSource: snapshot
    cacheHit: false
    missingMaster: true
    fallbackUsed: true
    status: pending (Live spec missing)
    note: Live-specific Claim spec is not yet enabled; once selectors stabilize and `test.fixme` is removed we will re-run to capture warning banner tone and server audit metadata.
