# ORCAフラグとtone推移メモ（RUN_ID=20251212T090000Z）

- ステージ: `VITE_DISABLE_MSW=1` + `VITE_DEV_PROXY_TARGET=http://100.102.17.40:8000/openDolphin/resources`（スクリーンショット: DocumentTimeline missingMaster と OrderConsole/OrcaSummary の `dataSourceTransition=server` 表示を Stage preview でキャプチャ済み）
- 目的: DocumentTimeline/OrderConsole/OrcaSummary/Patients の `missingMaster` と `dataSourceTransition=server` 表示を `auth-service` tone フラグで同期し、ORCA `cacheHit` で tone を切り替えながら `data-run-id` を carry over する UI を確認。

## 観測ログ（Stage/Preview）
| 時刻 | コンポーネント | cacheHit | missingMaster | dataSourceTransition | tone | aria-live | 補足 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 09:08:10Z | DocumentTimeline | false | true | snapshot→server | Warning (琥珀) | assertive | `auth-service.tone=server` で missingMaster banner 表示。`data-run-id=20251212T090000Z` を Reception へ carry over。 |
| 09:09:45Z | OrderConsole FilterBadge | false | true | server | Warning | polite | `dataSourceTransition.from=snapshot`、`to=server`、reason=`retry` を badge tooltip に記録し、audit meta 送出。 |
| 09:10:55Z | Patients header/row | true | false | server | Info (青) | polite | `cacheHit=true` 受信で tone を青へ。missingMaster バナーは透明化し、`aria-live=polite` で「マスタ取得済み」読み上げ。 |
| 09:12:20Z | OrcaSummary DataSourceBanner | true | false | server | Info | polite | `cacheHit` true で「マスタキャッシュ使用」表記、`audit.logUiState` に `cacheHit=true`/`dataSourceTransition=server`/`missingMaster=false` を添えた。 |

## スクリーンショット
- DocumentTimeline missingMaster banner + `aria-live=assertive`（Stage preview 画面のキャプチャ; `docs/web-client/ux/ux-documentation-plan.md` 新セクションで参照）
- OrderConsole/OrcaSummary の `dataSourceTransition=server` 表示直後のヘッダー＋Patients 一覧（同セクションで補足）

## 次の観測ポイント
1. Playwright `missingMaster` → `cacheHit` → `dataSourceTransition=server` の tone 遷移を再現するケースを追加し、`docs/web-client/ux/playwright-scenarios.md` に `auth-service` flag の切り替え経路を記載。
2. `audit.logUiState`/`audit.logOrcaQuery` に `action=poll` などで `tone`/`ariaLive` を透過しているかを API audit log からも確認。artifact は次 RUN_ID でも参照可能なよう `artifacts/webclient/ux-notes/` に保管。EOF
