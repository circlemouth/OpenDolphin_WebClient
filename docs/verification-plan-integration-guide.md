# verification-plan.md 統合手順（IDベース差し込み）

> 目的: `docs/verification-plan.md` の §5/§6 に、IDベースのサーバー機能一覧/トレーサビリティ表を差し込む手順を明文化する。

## 対象ファイル

- 差し込み先: `docs/verification-plan.md`
- 差し込み元: `/Users/Hayato/Documents/GitHub/OpenDolphin_WebClient/docs/server-modernization/server-modernized-api-traceability-insert-20260202.md`

## 差し込み手順（見出し・表フォーマット整合）

1. `docs/verification-plan.md` の **§5 サーバー機能一覧**を開く。
2. §5 の [PLACEHOLDER] を、差し込み元ファイルの **「サーバー機能一覧（差し込み本文案・ID版）」** セクションで置換する。
3. §5.1 の表フォーマットは以下で固定する（差し込み元と同一）:

| 機能ID | 機能名 | API / バッチ | 役割概要 | 依存先 | 備考 |
| --- | --- | --- | --- | --- | --- |

4. `docs/verification-plan.md` の **§6 トレーサビリティ表（画面 x サーバー機能）**を開く。
5. §6 の [PLACEHOLDER] を、差し込み元ファイルの **「トレーサビリティ表（画面 x サーバー機能）（差し込み本文案・ID版）」** セクションで置換する。
6. §6.1 の表フォーマットは以下で固定する（差し込み元と同一）:

| 画面ID | 画面名 | 機能ID | 機能名 | 依存区分 | 備考 |
| --- | --- | --- | --- | --- | --- |

7. 差し込み元の **「不足・要確認事項（差し込み用）」** は `docs/verification-plan.md` の **§10 要確認事項**へ移動し、箇条書きを追記する（重複は統合）。

## ID起番ルール（継続運用）

- 機能IDは `SVR-###`（3桁ゼロ埋め）。例: `SVR-001`。
- 画面IDは `SCR-###`（3桁ゼロ埋め）。例: `SCR-001`。
- 追加時は **既存の最大番号に +1** で連番を継続する（欠番は作らない）。
- トレーサビリティ表の機能IDは §5 の一覧と **同一ID** を使用する。

## 位置合わせの注意

- §5 / §6 の見出し名は変更しない（計画書全体の章立て固定）。
- 差し込み元の見出しは本文内に残さず、計画書の既存見出しへ統合する。
- 画面名は `Reception / Charts / Patients / Administration` の表記で統一する。
