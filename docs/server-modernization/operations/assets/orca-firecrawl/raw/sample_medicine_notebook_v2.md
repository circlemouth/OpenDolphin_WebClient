#!/usr/bin/ruby # -\*- coding: utf-8 -\*- #------ ����蒠��� require 'uri' require 'net/http' require 'json' Net::HTTP.version\_1\_2 HOST = "localhost" PORT = "8000" USER = "ormaster" PASSWD = "ormaster" CONTENT\_TYPE = "application/xml" req = Net::HTTP::Post.new("/api01rv2/medicinenotebookv2") BODY = < 01 1 373 False EOF req.content\_length = BODY.size req.content\_type = CONTENT\_TYPE req.body = BODY req.basic\_auth(USER, PASSWD) Net::HTTP.start(HOST, PORT) {|http| res = http.request(req) puts JSON.pretty\_generate(JSON.load(res.body)) }
