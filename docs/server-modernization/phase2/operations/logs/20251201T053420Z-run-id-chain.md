# 参照チェーン整理 (RUN_ID=20251201T053420Z)

期間: 2025-12-01 09:00 〜 2025-12-02 09:00 (JST)

## 確認した参照チェーン
1. `AGENTS.md`（Phase2 ガバナンス、Python 実行禁止、Legacy 資産の参照のみ、`server/` 改変不可、ORCA の接続情報を機微情報として扱うルール）。
2. `docs/web-client/README.md`（ログイン画面だけが稼働している現状、RUN_ID 共有・DOC_STATUS 連携のハブ）。
3. `docs/server-modernization/phase2/INDEX.md`（ORCA 接続トライアル/本番制限、RUN_ID の運用手順、DOC_STATUS とハブとの同期手順）。
4. `docs/managerdocs/PHASE2_MANAGER_ASSIGNMENT_OVERVIEW.md` / `PHASE2_WEB_CLIENT_EXPERIENCE_MANAGER_CHECKLIST.md`（AGENTS → README → INDEX → マネージャー資料の順で参照する運用を再確認）。

## 画面計画ドキュメントの現状
- Login 再構成: `docs/web-client/planning/phase2/LOGIN_REWORK_PLAN.md`（`LoginScreen.tsx` + `main.tsx` の単純化で `npm run build` が通るように再構成した唯一の稼働画面）。
- 空枠ドラフト: `docs/web-client/planning/phase2/screens/RECEPTION_SCREEN_PLAN.md`、`CHART_ENTRY_SCREEN_PLAN.md`、`CHART_ADMIN_SCREEN_PLAN.md`（受付・カルテ記入・カルテ管理の3画面の設計草案）。

現行 Web クライアントが Login 画面のみのため、上記 3 本は画面拡張候補であり、実装フェーズに入った段階で DOC_STATUS に RUN_ID `20251201T053420Z` を添えて各画面の計画を展開する。未計画の画面（例: Schedule/Patient Admin）については既存フォルダへの追加登録を優先する。

## 制約とルールの再確認
- Python スクリプトを勝手に走らせない。シェル／npm で完結する手段を取るか、明示指示を待つ。
- `server/` 下の旧サーバースクリプトや Legacy `client/`、`common/`、`ext_lib/` は変更禁止。参照用に留め、保守運用作業は不可。
- ORCA 接続情報は `docs/web-client/operations/mac-dev-login.local.md` を参照し、WebORCA トライアル以外へのアクセスや `curl --cert-type P12` の利用はしない。ORCA の CRUD 実測や API 状況は `docs/server-modernization/phase2/operations/ORCA_CONNECTIVITY_VALIDATION.md` / `ORCA_API_STATUS.md` から拾った情報を使う。
- DOC_STATUS 更新後は README・INDEX に同一 RUN_ID を記載し、証跡ログへのリンクを備考へ追記。RUN_ID の派生を使う場合は親 `20251201T053420Z` を明示する。

## 更新結果（2025-12-01）
- DOC_STATUS に `src/webclient_screens_plan/00_RUN_IDと参照チェーン整理.md` 行を追加し、備考へ RUN_ID=`20251201T053420Z` と本ログを記録（`docs/web-client/planning/phase2/DOC_STATUS.md`）。
- Web クライアント Hub に参照チェーン棚卸し完了と本ログのリンクを追記（`docs/web-client/README.md`）。
- Phase2 INDEX に同 RUN_ID での棚卸し完了メモと本ログへのリンクを追記（`docs/server-modernization/phase2/INDEX.md`）。
- Web クライアント Experience manager checklist に参照チェーン更新済みのメモを追加（`docs/managerdocs/PHASE2_WEB_CLIENT_EXPERIENCE_MANAGER_CHECKLIST.md`）。
- スクリーンプランメモの次アクションを「完了済み」に更新し、本ログとの同期を明記（`src/webclient_screens_plan/00_RUN_IDと参照チェーン整理.md`）。

## 次の証跡予定
- 追加画面（Reception/Charts/Patients/Schedule など）の計画メモと証跡ログを `docs/web-client/planning/phase2/screens`／`docs/web-client/planning/phase2/logs/` に追加する際は、本ログを親 RUN として参照チェーンを再同期する。
- 参照チェーンの棚卸しに派生 RUN_ID を使う場合は、本 RUN_ID=`20251201T053420Z` を親として備考と証跡の両方に明示する。
