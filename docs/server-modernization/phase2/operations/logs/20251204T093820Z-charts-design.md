# 20251204T093820Z Charts/Patients UX デザインログ

- RUN_ID チェーン: `20251204T093820Z`（親=`20251203T210000Z` の UX ドキュメント調査の派生）
- 参照チェーン: `AGENTS.md` → `docs/web-client/README.md` → `docs/server-modernization/phase2/INDEX.md` → `docs/managerdocs/PHASE2_MANAGER_ASSIGNMENT_OVERVIEW.md` → 本ログ → `src/outpatient_ux_modernization/04B1_WEBクライアントChartsPatientsUX設計.md`

## 1. 目的と成果
1. DocumentTimeline/OrderConsole/Patients の missingMaster バナーと `dataSourceTransition=server` の tone を `docs/web-client/ux/charts-claim-ui-policy.md`/`docs/web-client/ux/reception-schedule-ui-policy.md`/`docs/web-client/ux/patients-admin-ui-policy.md` と整合させる設計をまとめた。
2. ORCA `cacheHit`/`missingMaster`/`dataSourceTransition` フラグを Reception と同じ `tone`/`aria-live` ルールで伝播し、`OrderConsole`/`OrcaSummary`/`Patients` の state が `data-run-id` で同期するよう監査 meta を添える観点を残した。
3. Stage/Preview での観測項目を `artifacts/webclient/ux-notes/20251204T093820Z-charts-design.md` に記録し、`DocumentTimeline` の赤/琥珀/青バナー・OrderConsole の `FilterBadge`・Patients のヘッダーで `cacheHit`/`missingMaster` の転移を観測する手順を明示した。

## 2. 対応ドキュメント同期
- `src/outpatient_ux_modernization/04B1_WEBクライアントChartsPatientsUX設計.md` に RUN_ID=`20251204T093820Z` を起点とした traceability、ORCA flag の受信タイミング、Stage 観測チェックリストを記述。
- `artifacts/webclient/ux-notes/20251204T093820Z-charts-design.md` に Stage 実行時の観測項目・JSON キャプチャの要点・Playwright/a11y の引き継ぎポイントをまとめ、Implementation/検証チーム向けに共有。
- DOC_STATUS `Web クライアント UX/Features` 行（`docs/web-client/planning/phase2/DOC_STATUS.md`）を RUN_ID=`20251204T093820Z` と本ログ/アーティファクトのパスで更新し、次の RUN_ID を触る際は同じ行の備考を辿るよう整備。

## 3. 証跡・次ステップ
- 証跡: `src/outpatient_ux_modernization/04B1_WEBクライアントChartsPatientsUX設計.md` + `artifacts/webclient/ux-notes/20251204T093820Z-charts-design.md` + `docs/web-client/planning/phase2/DOC_STATUS.md`（UX 行） + 本ログ。
- 次ステップ: Playwright/a11y 担当は `cacheHit`/`missingMaster`/`dataSourceTransition` 変化を `aria-live=assertive`/`polite` で分離する自動検証を追加し、`audit.logUiState` に転送された値とバナーの色の一致を確認する。Implementation は Stage 観測で得られた `data-run-id` のキャプチャを共有して Reception 復帰時の carry over を確認し、`Patients` 画面の `missingMaster` tooltips を文言精査する。
