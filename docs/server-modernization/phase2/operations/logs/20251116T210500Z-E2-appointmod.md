# 20251116T210500Z-E2 `/orca14/appointmodv2` 実測ログ

- RUN_ID: `20251116T210500Z-E2`（親 RUN_ID=`20251116T111329Z`, EXT-02 ライン）
- 参照チェーン: `AGENTS.md` → `docs/web-client/README.md` → `docs/server-modernization/phase2/INDEX.md` → `PHASE2_ORCA_PHR_GAP_MANAGER_CHECKLIST.md` → 本ログ
- スコープ: 予約ラッパー `/orca14/appointmodv2` を Trial/ORMaster で再測し、`[Spec-based]` ラベル解除条件（実リクエストの HTTP200 証跡＋UI 連携可否）を確認する。
- 証跡ルート: `artifacts/orca-connectivity/20251116T210500Z-E2/appointmodv2/`（`trial/`, `ormaster/`, `dns/`）

## 1. Trial (weborca-trial.orca.med.or.jp)
- 実行日時: 2025-11-16 21:57 JST（UTC 12:57）
- コマンド: `curl -v -u <MASKED>:<MASKED> -H 'Content-Type: application/xml; charset=UTF-8' --data-binary @payloads/appointmod_trial.xml https://weborca-trial.orca.med.or.jp/orca14/appointmodv2?class=01`
- 応答: `HTTP/1.1 405 Method Not Allowed`（Allow=`OPTIONS, GET`）。body=`{"Code":405,"Message":"code=405, message=Method Not Allowed"}`。
- 判定: Trial 環境では POST が恒常的に拒否され、`[Spec-based]` の根拠（`trialsite.md#limit`）は維持。
- 証跡: `trial/response.xml`（HTTP body）、`trial/headers.txt`、`trial/curl_verbose.log`、`trial/http_status.txt`。

## 2. ORMaster (ormaster.orca.med.or.jp) 試行
- 目的: Plan `20251116T173000Z` で定義された ORMaster 直収による `[Spec-based]` 解除条件の確認。
- DNS: `nslookup ormaster.orca.med.or.jp` → `NXDOMAIN`。出力を `artifacts/orca-connectivity/20251116T210500Z-E2/dns/ormaster.nslookup.txt` に保存。
- `curl` 実行: `curl -v -u ormaster:ormaster ... https://ormaster.orca.med.or.jp/orca14/appointmodv2?class=01`。
- 結果: DNS 解決失敗（curl: (6) Could not resolve host）。`http_status.txt` は `000`（未到達）で HTTP レスポンスなし。
- 判定: CLI 環境から ORMaster へ到達できず、Spec-based 解除条件（Trial 405 の代替として ORMaster 200 を取得）が満たせない。DNS レコードの提示または VPN/ホワイトリストが必要。

## 3. 次アクション
1. `ormaster.orca.med.or.jp` への DNS ルートを Ops へ確認し、アクセス許可を得た後に再測（`RUN_ID=20251116T210500Z-E2` の派生を想定）。
2. Trial 側 405 を `external-api-gap-20251116T111329Z.md` EXT-02 に反映し、`ORCA_API_STATUS.md` No.2 の `[Spec-based]` を維持する旨を共有。
3. UI 連携（予約画面の REST ラッパー）は Trial 実測結果では変更なしのため、スクリーンショット取得は保留。
