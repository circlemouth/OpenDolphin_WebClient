#parse("common/header.vm")
#parse("common/karte_helpers.vm")
<p>本帳票はレセ電送信データの照合用です。下表の内容を ORCA 出力と突き合わせてください。</p>

#renderPatientOverview($context)

#renderSummaryTable($context)

<footer>
  出力日時: $context.generatedAt.format('yyyy年MM月dd日 HH:mm') / 担当: $context.attendingDoctor
</footer>

#parse("common/footer.vm")
