# Web クライアント向け環境変数サンプル
# API ベース URL（同一オリジン前提。未指定時は /api を使用）
VITE_API_BASE_URL=/api

# MSW 無効化フラグ（1 で MSW を無効化）
VITE_DISABLE_MSW=0

# 監査ログの送信先を分離する場合は指定（未使用ならコメントアウト）
# VITE_AUDIT_ENDPOINT=https://localhost:8080/opendolphin/audit

# HTTP タイムアウト（ミリ秒）
VITE_HTTP_TIMEOUT_MS=10000

# ネットワークエラー時の最大リトライ回数
VITE_HTTP_MAX_RETRIES=2

# Vite dev/preview のプロキシ接続先（ローカル検証時のみ使用。/api をこのオリジンへ中継）
VITE_DEV_PROXY_TARGET=http://localhost:8080
# HTTPS を無効化する場合のみ 0 を指定
VITE_DEV_USE_HTTPS=1

# ORCA マスター取得経路
# msw|snapshot|server を指定（デフォルトは msw）
WEB_ORCA_MASTER_SOURCE=msw
# サーバー経路を明示する場合のベース URL（未指定時は VITE_API_BASE_URL を利用）
VITE_ORCA_MASTER_BRIDGE=/api
# audit run id をテンプレートに反映する場合はここで上書き（未指定時は Bridge 側の設定または 20251124T073245Z）
VITE_ORCA_MASTER_BRIDGE_RUN_ID=20251124T073245Z
# 既存スナップショットに対応した version を env へ登録すると監査メタに透過できる
VITE_ORCA_MASTER_SNAPSHOT_VERSION=20251124T000000Z
