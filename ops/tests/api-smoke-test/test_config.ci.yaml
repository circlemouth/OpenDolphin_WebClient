# GitHub Actions 用の最小スモークテスト設定。
# docker-compose で起動した旧サーバー／モダナイズ版のベースライン比較を想定し、
# 認証ヘッダーのみで呼び出せるエンドポイントに限定している。

defaults:
  headers:
    userName: "1.3.6.1.4.1.9414.10.1:dolphin"
    password: "36cdf8b887a5cffc78dcd5c08991b993"
    clientUUID: "ci-smoke-test-client"
  expected_status: [200]
  compare:
    mode: auto
    ignore_fields: []

cases:
  - id: systemresource_1
    description: "システム疎通確認用の /dolphin エンドポイント"

  - id: serverinforesource_2
    description: "cloud.zero 設定のプレーンテキスト応答を比較"
