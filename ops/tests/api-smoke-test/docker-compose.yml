services:
  db:
    extends:
      file: ../base/docker-compose.yml
      service: db

  server:
    extends:
      file: ../legacy-server/docker-compose.yml
      service: server

  server-modernized:
    profiles: ["modernized"]
    extends:
      file: ../modernized-server/docker-compose.yml
      service: server-modernized
